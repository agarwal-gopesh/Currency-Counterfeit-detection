from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='This project classifies between real and fake indian currency notes with their denomination.',
    author='Gopesh',
    license='MIT',
)
